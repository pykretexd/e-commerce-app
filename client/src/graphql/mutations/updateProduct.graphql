mutation UpdateProduct(
  $count: Float!
  $price: Float!
  $title: String!
  $updateProductId: Int!
) {
  updateProduct(
    count: $count
    price: $price
    title: $title
    id: $updateProductId
  ) {
    ...regularProduct
  }
}
